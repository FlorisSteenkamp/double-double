// See 'str-to-dd.spec.ts' for tests


// function makeRandomDds(count: number) {
//     const strs: string[] = [];

//     for (let i=0; i<count; i++) {
//         const m = 4;
//         const exp = squares((m*i)+0)%300;

//         const v1 = squares((m*i)+1);
//         const v2 = squares((m*i)+1);

//         const v = v1*v2*(10**exp);
//     }
// }
